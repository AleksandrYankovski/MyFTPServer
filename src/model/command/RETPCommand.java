package model.command;

import model.reply.Reply;

public final class RETPCommand implements Command {


    @Override
    public String execute() {
        throw new IllegalStateException("not implemented yet");
    }
}
